//just calling all the variables so that the system understands them when they're stated later in the code

int pumpPin = 11; //first pump connects to ~11 for power

int pumpSpeed = 80; //first pump speed set @80 because suction requires min volt to perform

int conductivity;

int threshold = 1023; //threshold to be met by prototype

int pump2Pin = 10; //second pump connects to ~10 for power

int pump2Speed = 225; //second pump speed set at max b/c peristaltic requires more volt/+speed to perform
